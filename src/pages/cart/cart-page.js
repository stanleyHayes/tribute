import AuthLayout from "../../components/layout/auth-layout";
import {Container, Typography} from "@mui/material";

const CartPage = () => {
    return (
        <AuthLayout>
            <Container>
                <Typography variant="h4" align="center">Cart Page</Typography>
            </Container>
        </AuthLayout>
    )
}

export default CartPage;
