import AuthLayout from "../../components/layout/auth-layout";
import {Container, Typography} from "@mui/material";

const WishlistPage = () => {
    return (
        <AuthLayout>
            <Container>
                <Typography variant="h4" align="center">Wishlist Page</Typography>
            </Container>
        </AuthLayout>
    )
}

export default WishlistPage;
