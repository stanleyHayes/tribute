import AuthLayout from "../../components/layout/auth-layout";
import {Container, Typography} from "@mui/material";

const AuthCartPage = () => {
    return (
        <AuthLayout>
            <Container>
                <Typography variant="h4" align="center">AuthCart Page</Typography>
            </Container>
        </AuthLayout>
    )
}

export default AuthCartPage;
